/* config.js — one place to point the site at your backend.
   Local Docker setup: leave as-is.
   When you deploy the backend somewhere else, change this URL
   (e.g. "https://api.yourdomain.com/api"). */
window.TD_API_BASE = 'http://localhost:5000/api';
